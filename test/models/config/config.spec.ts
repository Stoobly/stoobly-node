import { describe, expect, it } from '@jest/globals';

import { ConfigResource } from '@core/src/core/http';
import { Config } from '@models/config';

describe('stoobly', () => {
  it('has config property', () => {
  });
});