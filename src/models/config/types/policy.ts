export enum MockPolicy {
  All = 'all',
  Found = 'found',
}

export enum RecordPolicy {
  All = 'all',
  Found = 'found',
  NotFound = 'not_found',
}

export enum TestPolicy {
  All = 'all',
  Found = 'found',
}

export enum ReplayPolicy {
  All = 'all',
}
